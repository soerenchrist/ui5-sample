_schema-version: 3.3.0
ID: toom_sample
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: toom_sample-srv
    type: nodejs
    path: gen/srv
    parameters:
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: toom_sample-db
      - name: toom_sample-auth
      - name: toom_sample-destination

  - name: toom_sample-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: toom_sample-db

  - name: toom_sample-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: toom_sample-html5-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: app/
      requires:
        - name: toom_sample-frontend
          artifacts:
            - toom_frontend.zip
          target-path: app/

  - name: toom_sample-frontend
    type: html5
    path: app/toom_frontend
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: toom_sample-destinations
    type: com.sap.application.content
    requires:
      - name: toom_sample-auth
        parameters:
          service-key:
            name: toom_sample-auth-key
      - name: toom_sample-html5-repo-host
        parameters:
          service-key:
            name: toom_sample-html5-repo-host-key
      - name: srv-api
      - name: toom_sample-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: toom_sample-html5-repository
              ServiceInstanceName: toom_sample-html5-repo-host
              ServiceKeyName: toom_sample-html5-repo-host-key
              sap.cloud.service: toom_sample.service
            - Name: toom_sample-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: toom_sample-auth
              ServiceKeyName: toom_sample-auth-key
              sap.cloud.service: toom_sample.service

resources:
  - name: toom_sample-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: toom_sample-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: toom_sample-${org}-${space}
        tenant-mode: dedicated
  - name: toom_sample-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: toom_sample-srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
              - Name: ui5
                URL: https://ui5.sap.com
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
  - name: toom_sample-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
